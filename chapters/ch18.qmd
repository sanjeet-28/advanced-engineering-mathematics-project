---
title: "CHAPTER 18: Complex Analysis and Potential Theory"
---

In Chapter 17 we developed the geometric approach of conformal mapping. This meant that, for a complex analytic function $w = f(z)$ defined in a domain $D$ of the $z$-plane, we associated with each point in $D$ a corresponding point in the $w$-plane. This gave us a conformal mapping (angle-preserving), except at critical points where $f'(z) = 0$.

Now, in this chapter, we shall apply conformal mappings to potential problems. This will lead to boundary value problems and many engineering applications in electrostatics, heat flow, and fluid flow. More details are as follows.

Recall that Laplace’s equation is one of the most important PDEs in engineering mathematics because it occurs in gravitation (Secs. 9.7, 12.11), electrostatics (Sec. 9.7), steady-state heat conduction (Sec. 12.5), incompressible fluid flow, and other areas. The theory of this equation is called potential theory (although “potential” is also used in a more general sense in connection with gradients (see Sec. 9.7)). Because we want to treat this equation with complex analytic methods, we restrict our discussion to the “two-dimensional case.” Then the potential $\Phi$ depends only on two Cartesian coordinates $x$ and $y$, and Laplace’s equation becomes

$$
abla^2 \Phi = rac{\partial^2 \Phi}{\partial x^2} + rac{\partial^2 \Phi}{\partial y^2} = 0$$

An important idea then is that its solutions are closely related to complex analytic functions as shown in Sec. 13.4. (Remark: We use the notation $\Phi$ and $\Psi$ to free $u$ and $v$, which will be needed in conformal mapping: $w = u + iv$.) This important relation is the main reason for using complex analysis in problems of physics and engineering.

We shall examine this connection between Laplace’s equation and complex analytic functions and illustrate it by modeling applications from electrostatics (Secs. 18.1, 18.2), heat conduction (Sec. 18.3), and hydrodynamics (Sec. 18.4). This in turn will lead to boundary value problems in two-dimensional potential theory. As a result, some of the functions of Chap. 17 will be used to transform complicated regions into simpler ones.

Section 18.5 will derive the important Poisson formula for potentials in a circular disk.

Section 18.6 will deal with harmonic functions, which, as you recall, are solutions of Laplace’s equation and have continuous second partial derivatives. In that section we will show how results on analytic functions can be used to characterize properties of harmonic functions.

**Prerequisite:** Chaps. 13, 14, 17.  
**References and Answers to Problems:** App. 1 Part D, App. 2.

## 18.1 Electrostatic Fields {#sec-electrostatic-fields}

The electrical force of attraction or repulsion between charged particles is governed by Coulomb’s law (see Sec. 9.7). This force is the gradient of a function $\Phi$ called the **electrostatic potential**. At any points free of charges, $\Phi$ is a solution of Laplace’s equation

$$
abla^2 \Phi = rac{\partial^2 \Phi}{\partial x^2} + rac{\partial^2 \Phi}{\partial y^2} = 0$$

The surfaces $\Phi = 	ext{const}$ are called **equipotential surfaces**. At each point $P$ at which the gradient of $\Phi$ is not the zero vector, it is perpendicular to the surface through $P$; that is, the electrical force has the direction perpendicular to the equipotential surface. (See also Secs. 9.7 and 12.11.)

The problems we shall discuss in this entire chapter are two-dimensional (for the reason just given in the chapter opening), that is, they model physical systems that lie in three-dimensional space (of course!), but are such that the potential is independent of one of the space coordinates, so that $\Phi$ depends only on two coordinates, which we call $x$ and $y$. Then Laplace’s equation becomes

$$rac{\partial^2 \Phi}{\partial x^2} + rac{\partial^2 \Phi}{\partial y^2} = 0$$ {#eq-sec181-1}

Equipotential surfaces now appear as equipotential lines (curves) in the $xy$-plane. Let us illustrate these ideas by a few simple examples.

### EXAMPLE 1 Potential Between Parallel Plates
Find the potential $\Phi$ of the field between two parallel conducting plates extending to infinity (Fig. 396), which are kept at potentials $U_1$ and $U_2$ respectively.

![Fig. 396. Potential in Example 1](../images/fig-18-01.png){#fig-18-01}

**Solution.** From the shape of the plates it follows that $\Phi$ depends only on $x$, and Laplace’s equation becomes $\Phi'' = 0$. By integrating twice we obtain $\Phi = ax + b$, where the constants $a$ and $b$ are determined by the given boundary values of $\Phi$ on the plates. For example, if the plates correspond to $x = -1$ and $x = 1$ and have potentials $U_1$ and $U_2$ respectively, the solution is

$$\Phi(x) = rac{1}{2}(U_2 - U_1)x + rac{1}{2}(U_2 + U_1)$$

The equipotential surfaces are parallel planes.

### EXAMPLE 2 Potential Between Coaxial Cylinders
Find the potential $\Phi$ between two coaxial conducting cylinders extending to infinity on both ends (Fig. 397) and kept at potentials $U_1$ and $U_2$ respectively.

![Fig. 397. Potential in Example 2](../images/fig-18-02.png){#fig-18-02}

**Solution.** Here $\Phi$ depends only on $r = \sqrt{x^2 + y^2}$ for reasons of symmetry, and Laplace’s equation [(5), Sec. 12.10] with $\Phi_	heta = 0$ and $\Phi_{zz} = 0$ becomes $r\Phi_{rr} + \Phi_r = 0$. By separating variables and integrating we obtain

$$\Phi_r = rac{a}{r}, \quad 	ext{so} \quad \Phi = a \ln r + b$$

and $a$ and $b$ are determined by the given values of $\Phi$ on the cylinders. Although no infinitely extended conductors exist, the field in our idealized conductor will approximate the field in a long finite conductor in that part which is far away from the two ends of the cylinders.

### EXAMPLE 3 Potential in an Angular Region
Find the potential $\Phi$ between the conducting plates in Fig. 398, which are kept at potentials $U_1$ (the lower plate) and $U_2$ (the upper plate) and make an angle $lpha$ where $0 < lpha \le \pi$. (In the figure we have $lpha = 120^\circ = 2\pi/3$.)

![Fig. 398. Potential in Example 3](../images/fig-18-03.png){#fig-18-03}

**Solution.** $\Phi$ is constant on rays $	heta = 	ext{const}$. It is harmonic since it is the imaginary part of an analytic function, $	ext{Ln } z = \ln r + i	heta$ (Sec. 13.7). Hence the solution is

$$\Phi(x, y) = a + b 	ext{Arg } z$$

with $a$ and $b$ determined from the two boundary conditions (given values on the plates):

$$U_1 = a + b\left(-rac{lpha}{2}ight), \quad U_2 = a + b\left(rac{lpha}{2}ight)$$

Thus $a = (U_2 + U_1)/2, b = (U_2 - U_1)/lpha$. The answer is

$$\Phi(x, y) = rac{1}{2}(U_2 + U_1) + rac{1}{lpha}(U_2 - U_1)	heta$$

### Complex Potential
Let $\Phi(x, y)$ be harmonic in some domain $D$ and $\Psi(x, y)$ a harmonic conjugate of $\Phi$ in $D$. (Note the change of notation from $u$ and $v$ of Sec. 13.4 to $\Phi$ and $\Psi$. From the next section on, we had to free $u$ and $v$ for use in conformal mapping.) Then

$$F(z) = \Phi(x, y) + i\Psi(x, y)$$ {#eq-sec181-2}

is an analytic function of $z = x + iy$. This function $F$ is called the **complex potential** corresponding to the real potential $\Phi$.

The use of $F$ has two advantages, a technical one and a physical one. Technically, $F$ is easier to handle than real or imaginary parts, in connection with methods of complex analysis. Physically, $\Psi(x, y)$ has a meaning. By conformality, the curves $\Psi(x, y) = 	ext{const}$ intersect the equipotential lines $\Phi(x, y) = 	ext{const}$ in the $xy$-plane at right angles [except where $F'(z) = 0$]. Hence they have the direction of the electrical force and, therefore, are called **lines of force**. They are the paths of moving charged particles (electrons in an electron microscope, etc.).

### EXAMPLE 4 Complex Potential
In Example 1, a conjugate is $\Psi = ay$. It follows that the complex potential is

$$F(z) = az + b = ax + b + iay$$

and the lines of force are horizontal straight lines parallel to the $x$-axis.

### EXAMPLE 5 Complex Potential
In Example 2 we have $\Phi = a\ln r + b$. A conjugate is $\Psi = a	heta = a	ext{Arg } z$. Hence the complex potential is

$$F(z) = a 	ext{Ln } z + b = a \ln |z| + b + ia 	ext{Arg } z$$

and the lines of force are straight lines through the origin. $F(z)$ may also be interpreted as the complex potential of a source line (a wire perpendicular to the $xy$-plane) whose trace in the $xy$-plane is the origin.

### EXAMPLE 6 Complex Potential
In Example 3 we get $F(z) = a - ib 	ext{Ln } z$ by noting that $i	ext{Ln } z = i \ln |z| - 	ext{Arg } z$, multiplying this by $-b$ and adding $a$:

$$F(z) = a + b 	ext{Arg } z - ib \ln |z|$$

We see from this that the lines of force are concentric circles $|z| = 	ext{const}$.

### Superposition
More complicated potentials can often be obtained by superposition.

### EXAMPLE 7 Potential of a Pair of Source Lines (a Pair of Charged Wires)
Determine the potential of a pair of oppositely charged source lines of the same strength at the points $c$ and $-c$ on the real axis.

**Solution.** From Examples 2 and 5 it follows that the potential of each of the source lines is $\Phi_1 = K\ln |z - c|$ and $\Phi_2 = -K\ln |z + c|$ respectively. Here the real constant $K$ measures the strength (amount of charge). These are the real parts of the complex potentials $F_1(z) = K	ext{Ln}(z - c)$ and $F_2(z) = -K	ext{Ln}(z + c)$. Hence the complex potential of the combination of the two source lines is

$$F(z) = F_1(z) + F_2(z) = K[	ext{Ln}(z - c) - 	ext{Ln}(z + c)] = K 	ext{Ln}\left(rac{z - c}{z + c}ight)$$ {#eq-sec181-3}

The equipotential lines are the curves $\Phi = 	ext{Re } F(z) = K \ln |(z - c)/(z + c)| = 	ext{const}$, thus

$$\left| rac{z - c}{z + c} ight| = 	ext{const}$$

These are circles, as you may show by direct calculation. The lines of force are $\Psi = 	ext{Im } F(z) = K[	ext{Arg}(z - c) - 	ext{Arg}(z + c)] = 	ext{const}$. We write this briefly (see @fig-18-04 and @fig-18-05):

$$\Psi = K(	heta_1 - 	heta_2) = 	ext{const}$$

![Fig. 399. Arguments in Example 7](../images/fig-18-04.png){#fig-18-04}

![Fig. 400. Equipotential lines and lines of force (dashed) in Example 7](../images/fig-18-05.png){#fig-18-05}

Now $	heta_1 - 	heta_2$ is the angle between the line segments from $z$ to $c$ and $-c$. Hence the lines of force are the curves along each of which the line segment $S: -c \le x \le c$ appears under a constant angle. These curves are the totality of circular arcs over $S$, as is known from elementary geometry. Hence the lines of force are circles. Figure 400 shows some of them together with some equipotential lines.

In addition to the interpretation as the potential of two source lines, this potential could also be thought of as the potential between two circular cylinders whose axes are parallel but do not coincide, or as the potential between two equal cylinders that lie outside each other, or as the potential between a cylinder and a plane wall. Explain this using Figure 400.

The idea of the complex potential as just explained is the key to a close relation of potential theory to complex analysis and will recur in heat flow and fluid flow.

### PROBLEM SET 18.1 {#sec-ps18-1}

#### 1–4 COAXIAL CYLINDERS
Find and sketch the potential between two coaxial cylinders of radii $r_1$ and $r_2$ having potential $U_1$ and $U_2$ respectively.
1. $r_1 = 2.5	ext{ mm}, r_2 = 4.0	ext{ cm}, U_1 = 0	ext{ V}, U_2 = 220	ext{ V}$.
2. $r_1 = 1	ext{ cm}, r_2 = 2	ext{ cm}, U_1 = 400	ext{ V}, U_2 = 0	ext{ V}$.
3. $r_1 = 10	ext{ cm}, r_2 = 1	ext{ m}, U_1 = 10	ext{ kV}, U_2 = -10	ext{ kV}$.
4. If $r_1 = 2	ext{ cm}$ and $r_2 = 6	ext{ cm}$, and the potentials are $U_1 = 300	ext{ V}$ and $U_2 = 100	ext{ V}$ respectively, is the potential at $r = 4	ext{ cm}$ equal to 200 V? Less? More? Answer without calculation. Then calculate and explain.

#### 5–7 PARALLEL PLATES
Find and sketch the potential between the parallel plates having potentials $U_1$ and $U_2$. Find the complex potential.
5. Plates at $x = -5	ext{ cm}$ and $x = 5	ext{ cm}$ at potentials $U_1 = 250	ext{ V}$ and $U_2 = 500	ext{ V}$ respectively.
6. Plates at $y = x$ and $y = x + k$ at potentials $U_1 = 0	ext{ V}$ and $U_2 = 220	ext{ V}$ respectively.
7. Plates at $x_1 = 12	ext{ cm}, x_2 = 24	ext{ cm}$ at potentials $U_1 = 20	ext{ kV}, U_2 = 8	ext{ kV}$ respectively.

#### 8–15 OTHER CONFIGURATIONS
8. **CAS EXPERIMENT. Complex Potentials.** Graph the equipotential lines and lines of force in (a)–(d) (four graphs, and on the same axes). Then explore further complex potentials of your choice with the purpose of discovering configurations that might be of practical interest.  
   (a) $F(z) = z^2$ &emsp; (b) $F(z) = iz^2$ &emsp; (c) $F(z) = 1/z$ &emsp; (d) $F(z) = i/z$.
9. **Argument.** Show that $\Phi = rac{1}{\pi}	ext{Arg}\,z = rac{1}{\pi}rctan(y/x)$ is harmonic in the upper half-plane and satisfies the boundary condition $\Phi(x, 0) = 1$ if $x < 0$ and $0$ if $x > 0$. Show that the corresponding complex potential is $F(z) = -rac{i}{\pi}	ext{Ln}\,z$.
10. **Conformal mapping.** Map the upper $z$-half-plane onto the unit disk $|w| \le 1$ so that $-1, 0, 1$ are mapped onto $-i, 1, i$, respectively. What are the boundary conditions on the disk resulting from the potential in Prob. 9? What is the potential at $w = 0$?
11. **Text Example 7.** Verify, by calculation, that the equipotential lines in Example 7 are circles.
12. Find and sketch the potential between the axes (potential 500 V) and the hyperbola $xy = 4$ (potential 100 V).
13. **Arccos.** Show that $F(z) = 	ext{arccos } z$ gives the potential of a slit in Fig. 401.
    
    ![Fig. 401. Slit](../images/fig-18-06.png){#fig-18-06}
14. **Arccos.** Show that $F(z)$ in Prob. 13 gives the potentials in Fig. 402.
    
    ![Fig. 402. Other apertures](../images/fig-18-07.png){#fig-18-07}
15. **Sector.** Find the real and complex potentials in the sector $-\pi/6 \le 	heta \le \pi/6, |z| \le 1$ kept at 0 V, and the curve $x^3 - 3xy^2 = 1$ kept at 220 V.

## 18.2 Use of Conformal Mapping. Modeling {#sec-conformal-modeling}

We have just explored the close relation between potential theory and complex analysis. This relationship is so close because complex potentials can be modeled in complex analysis. In this section we shall explore the close relation that results from the use of conformal mapping in modeling and solving boundary value problems for the Laplace equation. The process consists of finding a solution of the equation in some domain, assuming given values on the boundary (Dirichlet problem, see also Sec. 12.6). The key idea is then to use conformal mapping to map a given domain onto one for which the solution is known or can be found more easily. This solution thus obtained is then mapped back to the given domain. The reason this approach works is due to Theorem 1, which asserts that harmonic functions remain harmonic under conformal mapping:

### THEOREM 1 Harmonic Functions Under Conformal Mapping
Let $\Phi^*(u, v)$ be harmonic in a domain $D^*$ in the $w$-plane. Suppose that $w = f(z) = u(x, y) + iv(x, y)$ is analytic in a domain $D$ in the $z$-plane and maps $D$ conformally onto $D^*$. Then the function

$$\Phi(x, y) = \Phi^*(u(x, y), v(x, y))$$ {#eq-sec182-1}

is harmonic in $D$.

**PROOF** The composite of analytic functions is analytic, as follows from the chain rule. Hence, taking a harmonic conjugate $\Psi^*(u, v)$ of $\Phi^*$, and forming the analytic function $F^*(w) = \Phi^*(u, v) + i\Psi^*(u, v)$, we conclude that $F(z) = F^*(f(z))$ is analytic in $D$. Hence its real part $\Phi(x, y) = 	ext{Re } F(z) = 	ext{Re } F^*(f(z))$ is harmonic in $D$. This completes the proof. $lacksquare$

We mention without proof that if $D^*$ is simply connected (Sec. 14.2), then a harmonic conjugate of $\Phi^*$ exists. Another proof of Theorem 1 without the use of a harmonic conjugate is given in App. 4.

### EXAMPLE 1 Potential Between Noncoaxial Cylinders
Model the electrostatic potential between the cylinders $C_1: |z| = 1$ and $C_2: |z - 2/5| = 2/5$ in Fig. 403. Then give the solution for the case that $C_1$ is grounded, and $C_2$ has the potential 110 V.

![Fig. 403. Example 1: z-plane](../images/fig-18-08.png){#fig-18-08}

![Fig. 404. Example 1: w-plane](../images/fig-18-09.png){#fig-18-09}

**Solution.** We map the unit disk $|z| \le 1$ onto the unit disk $|w| \le 1$ in such a way that $C_2$ is mapped onto some cylinder $C_2^*: |w| = r_0$ coaxial with the unit circle. A linear fractional transformation mapping the unit disk onto the unit disk is

$$w = rac{z - b}{bz - 1}$$ {#eq-sec182-2}

where we have chosen $b$ real without restriction. Equation (2) is of no immediate help here because centers of circles do not map onto centers of the images, in general. However, we now have two free constants $b$ and $r_0$ and shall succeed by imposing two reasonable conditions, namely, that $z_1 = 0$ and $z_2 = 4/5$ (Fig. 403) should be mapped onto $w_1 = r_0$ and $w_2 = -r_0$ (Fig. 404), respectively. This gives by (2)

$$r_0 = b, \quad -r_0 = rac{4/5 - b}{4b/5 - 1}$$

and with this, $b^2 - 5b/2 + 1 = 0$, a quadratic equation in $b$ with solutions $b = 2$ (no good because $|b| < 1$) and $b = 1/2$. Hence our mapping function (2) with $b = 1/2$ becomes

$$w = rac{2z - 1}{z - 2}$$ {#eq-sec182-3}

and $r_0 = 1/2$.

From Example 5 in Sec. 18.1, writing $w$ for $z$, we have as the complex potential in the $w$-plane the function $F^*(w) = a 	ext{Ln } w + k$ and from this the real potential $\Phi^*(u, v) = a\ln |w| + k$. This is our model. We now determine $a$ and $k$ from the boundary conditions. If $|w| = 1$, then $\Phi^* = 0$; hence $a\ln 1 + k = 0$, which gives $k = 0$. If $|w| = r_0 = 1/2$, then $\Phi^* = 110$; hence $a\ln(1/2) = 110$, which gives $a = -110/\ln 2 pprox -158.7$. Substitution of (3) now gives the desired solution in the given domain in the $z$-plane

$$F(z) = F^*(f(z)) = a 	ext{Ln}\left(rac{2z - 1}{z - 2}ight)$$

The real potential is

$$\Phi(x, y) = a \ln \left| rac{2z - 1}{z - 2} ight| pprox -158.7 \ln \left| rac{2z - 1}{z - 2} ight|$$

Can we “see” this result? Well, $\Phi(x, y) = 	ext{const}$ if and only if $|w| = 	ext{const}$, that is, $|(2z - 1)/(z - 2)| = 	ext{const}$. These circles are images of circles in the $z$-plane because the inverse of a linear fractional transformation is linear fractional, and any such mapping maps circles onto circles (or straight lines). Similarly for the rays $	ext{arg } w = 	ext{const}$. Hence the equipotential lines are circles, and the lines of force are circular arcs (dashed in Fig. 404). These two families of curves intersect orthogonally, that is, at right angles, as shown in Fig. 404.

### EXAMPLE 2 Potential Between Two Semicircular Plates
Model the potential between two semicircular plates $P_1$ and $P_2$ in Fig. 405 having potentials $-3000	ext{ V}$ and $3000	ext{ V}$, respectively. Use Example 3 in Sec. 18.1 and conformal mapping.

![Fig. 405. Example 2: z-plane](../images/fig-18-10.png){#fig-18-10}

![Fig. 406. Example 2: w-plane](../images/fig-18-11.png){#fig-18-11}

**Solution.** 
**Step 1.** We map the unit disk in Fig. 405 onto the right half of the $w$-plane (Fig. 406) by using the linear fractional transformation in Example 3, Sec. 17.3:

$$w = rac{1 + z}{1 - z}$$

The boundary $|z| = 1$ is mapped onto the boundary $u = 0$ (the $v$-axis), with $z = -1, i, 1$ going onto $w = 0, i, \infty$ respectively, and $z = -i$ onto $w = -i$. Hence the upper semicircle of $|z| = 1$ is mapped onto the upper half, and the lower semicircle onto the lower half of the $v$-axis, so that the boundary conditions in the $w$-plane are as indicated in Fig. 406.

**Step 2.** We determine the potential in the right half-plane of the $w$-plane. Example 3 in Sec. 18.1 with $U_1 = -3000, U_2 = 3000$, and $lpha = \pi$ yields

$$\Phi^*(u, v) = rac{6000}{\pi}\phi = rac{6000}{\pi}	ext{Arg } w$$

On the positive half of the imaginary axis this equals 3000, and on the negative half $-3000$ as it should be. $\Phi^*$ is the real part of the complex potential

$$F^*(w) = -rac{6000 i}{\pi}	ext{Ln } w$$

**Step 3.** We substitute the mapping function into $F^*$ to get the complex potential in Fig. 405 in the form

$$F(z) = -rac{6000 i}{\pi}	ext{Ln}\left(rac{1 + z}{1 - z}ight)$$

The real part of this is the potential we wanted to determine:

$$\Phi(x, y) = rac{6000}{\pi}	ext{Arg}\left(rac{1 + z}{1 - z}ight)$$

As in Example 1 we conclude that the equipotential lines are circular arcs because they correspond to $	ext{Arg } w = 	ext{const}$. Also, they are rays from $0$ to the images of $z = -1$ and $z = 1$, respectively. Hence the equipotential lines all have $-1$ and $1$ (the points where the boundary potential jumps) as their endpoints (Fig. 405). The lines of force are circular arcs, too, and since they must be orthogonal to the equipotential lines, their centers can be obtained as intersections of tangents to the unit circle with the $x$-axis.

Further examples can easily be constructed. Just take any mapping $w = f(z)$ in Chap. 17, a domain $D$ in the $z$-plane, its image $D^*$ in the $w$-plane, and a potential $\Phi^*$ in $D^*$. Then $\Phi(x, y) = \Phi^*(u(x, y), v(x, y))$ gives a potential in $D$.

### PROBLEM SET 18.2 {#sec-ps18-2}

1. **Derivation of (3) from (2).** Verify the quadratic equation for $b$ and its solutions in Example 1.
2. **Second proof.** Give the details of the steps in App. 4 for the proof of Theorem 1 without using harmonic conjugates.
3. **Application of Theorem 1.** Find the potential in the region $R$ in the first quadrant of the $z$-plane bounded by the axes (having potential 0) and the hyperbola $xy = 1$ (having potential $U_2$) by mapping $R$ onto a suitable infinite strip. Show that $\Phi$ is harmonic. What are its boundary values?
4. Let $F^*(w) = 	ext{Ln } w$ and $w = f(z) = e^z$. Find $F(z)$. What are its boundary values?
5. **CAS PROJECT. Graphing Potential Fields.** Graph equipotential lines (a) in Example 1 of the text, (b) if the complex potential is $F(z) = z^2$, $iz^2$, $e^z$, $	ext{Ln } z$. (c) Graph the equipotential surfaces for $F(z) = 	ext{Ln } z$ as cylinders in space.
6. Apply Theorem 1 to $\Phi^*(u, v) = u^2 - v^2$ and $w = f(z) = e^z$, showing that the resulting potential is harmonic.
7. **Rectangle.** Let $D: 0 \le x \le \pi/2, 0 \le y \le 1$ and $w = \sin z$. Let $\Phi^*(u, v) = u^2 - v^2$. What is the corresponding potential in $D$? What are its boundary values? Sketch $D$ and $D^*$.
8. **Conjugate potential.** What happens in Prob. 7 if you replace the potential by its conjugate harmonic?
9. **Translation.** What happens in Prob. 7 if we replace $z$ by $z + i\pi/2$?
10. **Noncoaxial Cylinders.** Find the potential between the cylinders $C_1: |z| = 1$ (potential 0) and $C_2: |z - c| = c$ (potential $U_2$), where $0 < c < 1/2$. Sketch or graph equipotential lines and their orthogonal trajectories for $c = 1/4$. Can you guess how the graph changes if you increase $c$?
11. **On Example 2.** Verify the calculations in Example 2.
12. Show that in Example 2 the $y$-axis is mapped onto the unit circle in the $w$-plane.
13. At $z = \pm 1$ in Fig. 405 the tangents to the equipotential lines as shown make equal angles. Why?
14. Figure 405 gives the impression that the potential on the $y$-axis changes more rapidly near 0 than near $\pm 1$. Can you verify this?
15. **Angular region.** By applying a suitable conformal mapping, obtain from Fig. 406 the potential in the sector $-\pi/4 \le 	ext{Arg } z \le \pi/4$ such that $\Phi = -3	ext{ kV}$ if $	ext{Arg } z = -\pi/4$ and $\Phi = 3	ext{ kV}$ if $	ext{Arg } z = \pi/4$.
16. Solve Prob. 15 if the sector is $-\pi/8 \le 	ext{Arg } z \le \pi/8$.
17. **Another extension of Example 2.** Find the linear fractional transformation that maps $|z| \le 1$ onto $|Z| \le 1$ with $z = -1, 1$ being mapped onto $Z_1 = 0.6 + 0.8i, Z_2 = -0.6 - 0.8i$. Show that $z = 0$ is mapped onto $Z = i/2$. Show that the equipotential lines look as shown in Fig. 407.

    ![Fig. 407. Problem 17](../images/fig-18-12.png){#fig-18-12}

#### Basic Comment on Modeling
We formulated the examples in this section as models on the electrostatic potential. It is quite important to realize that this is accidental. We could equally well have phrased everything in terms of (time-independent) heat flow; then instead of voltages we would have had temperatures, the equipotential lines would have become isotherms (lines of constant temperature), and the lines of the electrical force would have become lines along which heat flows from higher to lower temperatures (more on this in the next section). Or we could have talked about fluid flow; then the electrostatic lines of force would have become streamlines (more on this in Sec. 18.4). What we again see here is the unifying power of mathematics: different phenomena and systems from different areas in physics having the same types of model can be treated by the same mathematical methods. What differs from area to area is just the kinds of problems that are of practical interest.

## 18.3 Heat Problems {#sec-heat-problems}

Heat conduction in a body of homogeneous material is modeled by the heat equation

$$rac{\partial T}{\partial t} = c^2 
abla^2 T$$

where the function $T$ is temperature, $t$ is time, and $c^2$ is a positive constant (specific to the material of the body; see Sec. 12.6).

Now if a heat flow problem is steady, that is, independent of time, we have $\partial T/\partial t = 0$. If it is also two-dimensional, then the heat equation reduces to Laplace's equation

$$
abla^2 T = rac{\partial^2 T}{\partial x^2} + rac{\partial^2 T}{\partial y^2} = 0$$ {#eq-sec183-1}

Thus we have shown that we can model a two-dimensional steady heat flow problem by Laplace’s equation.

Furthermore we can treat this heat flow problem by methods of complex analysis, since $T(x, y)$ is the real part of the complex heat potential

$$F(z) = T(x, y) + i\Psi(x, y)$$

We call $T$ the heat potential. The curves $T(x, y) = 	ext{const}$ are called **isotherms**, which means lines of constant temperature. The curves $\Psi(x, y) = 	ext{const}$ are called **heat flow lines** because heat flows along them from higher temperatures to lower temperatures.

It follows that all the examples considered so far (Secs. 18.1, 18.2) can now be reinterpreted as problems on heat flow. The electrostatic equipotential lines now become isotherms and the lines of electrical force become lines of heat flow, as in the following two problems.

### EXAMPLE 1 Temperature Between Parallel Plates
Find the temperature between two parallel plates $x = 0$ and $x = d$ (Fig. 408) having temperatures $0$ and $100^\circ	ext{C}$ respectively.

![Fig. 408. Example 1](../images/fig-18-13.png){#fig-18-13}

**Solution.** As in Example 1 of Sec. 18.1 we conclude that $T(x, y) = ax + b$. From the boundary conditions, $T(0, y) = b = 0$ and $T(d, y) = ad = 100$. The answer is

$$T(x, y) = rac{100}{d} x$$

The corresponding complex potential is $F(z) = (100/d)z$. Heat flows horizontally in the negative $x$-direction along the lines $y = 	ext{const}$.

### EXAMPLE 2 Temperature Distribution Between a Wire and a Cylinder
Find the temperature field around a long thin wire of radius $r_1 = 1	ext{ mm}$ that is electrically heated to $T_1 = 500^\circ	ext{F}$ and is surrounded by a circular cylinder of radius $r_2 = 100	ext{ mm}$ which is kept at temperature $T_2 = 60^\circ	ext{F}$ by cooling it with air. See Fig. 409. (The wire is at the origin of the coordinate system.)

![Fig. 409. Example 2](../images/fig-18-14.png){#fig-18-14}

**Solution.** $T$ depends only on $r$, for reasons of symmetry. Hence, as in Sec. 18.1 (Example 2), $T(x, y) = a\ln r + b$. The boundary conditions are

$$T_1 = a\ln 1 + b = 500, \quad T_2 = a\ln 100 + b = 60$$

Hence $b = 500$ (since $\ln 1 = 0$) and $a = (60 - 500)/\ln 100 pprox -95.54$. The answer is

$$T(x, y) = 500 - 95.54 \ln r \quad [^\circ	ext{F}]$$

The isotherms are concentric circles. Heat flows from the wire radially outward to the cylinder.

Mathematically the calculations remain the same in the transition to another field of application. Physically, new problems may arise, with boundary conditions that would make no sense physically or would be of no practical interest. This is illustrated by the next two examples.

### EXAMPLE 3 A Mixed Boundary Value Problem
Find the temperature distribution in the region in Fig. 410 (cross section of a solid quarter-cylinder), whose vertical portion of the boundary is at $20^\circ	ext{C}$, the horizontal portion at $50^\circ	ext{C}$, and the circular portion is insulated.

![Fig. 410. Example 3](../images/fig-18-15.png){#fig-18-15}

**Solution.** The insulated portion of the boundary must be a heat flow line, since, by the insulation, heat is prevented from crossing such a curve, hence heat must flow along the curve. Thus the isotherms must meet such a curve at right angles. Since $T$ is constant along an isotherm, this means that

$$rac{\partial T}{\partial n} = 0$$ {#eq-sec183-2}

along an insulated portion of the boundary. Here $\partial T/\partial n$ is the normal derivative of $T$, that is, the directional derivative (Sec. 9.7) in the direction normal (perpendicular) to the insulated boundary. Such a problem in which $T$ is prescribed on one portion of the boundary and $\partial T/\partial n = 0$ on the other portion is called a **mixed boundary value problem**.

In our case, the normal direction to the insulated circular boundary curve is the radial direction toward the origin. Hence (2) becomes $\partial T/\partial r = 0$, meaning that along this curve the solution must not depend on $r$. Now $T(x, y) = a + b 	ext{Arg } z = a + b	heta$ satisfies (1), as well as this condition, and is constant ($50^\circ	ext{C}$ and $20^\circ	ext{C}$) on the straight portions of the boundary. Hence the solution is of the form $T(x, y) = a + b	heta$. The boundary conditions yield $T(0) = a = 50$ and $T(\pi/2) = a + b\pi/2 = 20$. This gives $b = -60/\pi$. The temperature distribution is

$$T(x, y) = 50 - rac{60}{\pi}	heta = 50 - rac{60}{\pi}	ext{arctan}\left(rac{y}{x}ight)$$

The isotherms are portions of rays $	heta = 	ext{const}$. Heat flows from the $x$-axis along circles $r = 	ext{const}$ to the $y$-axis.

### EXAMPLE 4 Another Mixed Boundary Value Problem in Heat Conduction
Find the temperature field in the upper half-plane when the $x$-axis is kept at $T = 0^\circ	ext{C}$ for $x < -1$, is insulated for $-1 < x < 1$, and is kept at $T = 20^\circ	ext{C}$ for $x > 1$ (Fig. 411).

![Fig. 411. Example 4: z-plane](../images/fig-18-16.png){#fig-18-16}

![Fig. 412. Example 4: w-plane](../images/fig-18-17.png){#fig-18-17}

**Solution.** We map the half-plane in Fig. 411 onto the vertical strip in Fig. 412, find the temperature $T^*(u, v)$ there, and map it back to get the temperature $T(x, y)$ in the half-plane. The idea of using that strip is suggested by Fig. 391 in Sec. 17.4 with the roles of $z$ and $w$ interchanged. The figure shows that $z = \sin w$ maps our present strip onto our half-plane in Fig. 411. Hence the inverse function

$$w = f(z) = 	ext{arcsin } z$$

maps that half-plane onto the strip in the $w$-plane. This is the mapping function that we need according to Theorem 1 in Sec. 18.2.

The insulated segment $-1 < x < 1$ on the $x$-axis maps onto the segment $-\pi/2 < u < \pi/2$ on the $u$-axis. The rest of the $x$-axis maps onto the two vertical boundary portions $u = -\pi/2, v \ge 0$ and $u = \pi/2, v \ge 0$ of the strip. This gives the transformed boundary conditions in Fig. 412 for $T^*(u, v)$, where $\partial T^*/\partial v = 0$ on the insulated horizontal boundary, because $v$ is a coordinate normal to that segment.

Similarly to Example 1 we obtain

$$T^*(u, v) = 10 + rac{20}{\pi} u$$

which satisfies all the boundary conditions. This is the real part of the complex potential $F^*(w) = 10 + (20/\pi)w$. Hence the complex potential in the $z$-plane is

$$F(z) = F^*(f(z)) = 10 + rac{20}{\pi}	ext{arcsin } z$$

and $T(x, y) = 	ext{Re } F(z)$ is the solution. The isotherms $T = 	ext{const}$ are $u = 	ext{const}$ in the strip and the hyperbolas in the $z$-plane, perpendicular to which heat flows along the dashed ellipses from the $x > 1$ portion to the cooler $x < -1$ portion of the boundary, a physically very reasonable result.

### PROBLEM SET 18.3 {#sec-ps18-3}

1. **Parallel plates.** Find the temperature $T(x, y)$ between the plates $y = 0$ and $y = d$ kept at $100^\circ	ext{C}$ and $20^\circ	ext{C}$, respectively. (i) Proceed directly. (ii) Use Example 1 and a suitable mapping.
2. **Infinite plate.** Find the temperature and the complex potential in an infinite plate with edges $y = x - 4$ and $y = x + 4$ kept at $0^\circ	ext{C}$ and $40^\circ	ext{C}$, respectively (Fig. 413). In what case will this be an approximate model?

   ![Fig. 413. Problem 2: Infinite plate](../images/fig-18-18.png){#fig-18-18}

3. **CAS PROJECT. Isotherms.** Graph isotherms and lines of heat flow in Examples 2–4. Can you see from the graphs where the heat flow is very rapid?

#### 4–18 TEMPERATURE $T(x, y)$ IN PLATES
Find the temperature distribution and the complex potential in the given thin metal plate whose faces are insulated and whose edges are kept at the indicated temperatures or are insulated as shown.

4. Plate bounded by $x = -1, x = 1$ kept at $T = 0^\circ	ext{C}$ and $T = 100^\circ	ext{C}$ respectively, with horizontal edges insulated.
5. Sector $0 \le 	heta \le \pi/3$, $r \le 1$, with $	heta = 0$ at $T = 50^\circ	ext{C}$, $	heta = \pi/3$ at $T = 20^\circ	ext{C}$, and circular boundary $r = 1$ insulated.
6. Semi-infinite strip $0 \le x \le \pi, y \ge 0$, with bottom edge $y = 0$ at $T = 100^\circ	ext{C}$, and vertical edges $x = 0$ and $x = \pi$ at $T = 0^\circ	ext{C}$.
7. Wedge $0 \le 	heta \le \pi/4$ with $	heta = 0$ insulated, $	heta = \pi/4$ at $T = 60^\circ	ext{C}$, and circular boundary $r = 1$ at $T = -20^\circ	ext{C}$.
8. Infinite plate with a slit along the segment $-1 \le x \le 1, y = 0$, where the slit is insulated, and the upper half-plane is kept at $T = 200^\circ	ext{C}$ for $x > 1$ and $T = 0^\circ	ext{C}$ for $x < -1$.
9. Upper half-plane with boundary values on the $x$-axis: $T = 200^\circ	ext{C}$ for $x > 1$, $T = 0^\circ	ext{C}$ for $-1 < x < 1$, and $T = 200^\circ	ext{C}$ for $x < -1$.
10. Semi-infinite plate $x \ge 0$, with the segment $0 \le y \le 1$ of the $y$-axis insulated, the rest of the $y$-axis at $T = 0^\circ	ext{C}$, and the boundary curve $y = 10/x$ kept at $T = 200^\circ	ext{C}$.
11. First quadrant of the $z$-plane with $y$-axis kept at $T(0, y) = -30^\circ	ext{C}$, the segment $0 < x < 1$ of the $x$-axis insulated, and the $x$-axis for $x > 1$ kept at $T = 100^\circ	ext{C}$. Hint. Use Example 4.
12. Figure 410, but the circular boundary has temperature $T = 50^\circ	ext{C}$, and the horizontal and vertical boundaries are insulated. Hint. Apply $w = \cosh z$ or similar mapping.
13. Bounded plate $0 \le x \le \pi, 0 \le y \le \pi$, with top and bottom edges at $T = 100^\circ	ext{C}$ and $T = 0^\circ	ext{C}$, and vertical edges at $T = 0^\circ	ext{C}$.
14. Bounded plate $0 \le x \le 1, 0 \le y \le 1$, with top edge $y = 1$ at $T = 100^\circ	ext{C}$, and the other three edges at $T = 0^\circ	ext{C}$.
15. Semi-infinite strip $x \ge 0, 0 \le y \le \pi$, with vertical edge $x = 0$ at $T = T_1$ for $0 < y < \pi/2$ and $T = T_2$ for $\pi/2 < y < \pi$, and horizontal edges insulated.
16. Plate bounded by $y = \sqrt{3}x$ and $y = -\sqrt{3}x$ for $x \ge 0$, with boundaries at $T = 40^\circ	ext{C}$ and $T = 20^\circ	ext{C}$ respectively, and the outer circular boundary at $r = 4$ insulated.
17. Bounded quarter-disk $r \le 1, 0 \le 	heta \le \pi/2$, with radial boundaries insulated and the circular boundary kept at $T = 100\cos^2	heta$.
18. Solve Prob. 17 if the circular boundary is kept at $T = 100\sin 2	heta$.

#### 19–20 INTERPRETATION
19. Formulate Prob. 11 in terms of electrostatics.
20. Interpret Prob. 17 in Sec. 18.2 as a heat problem, with boundary temperatures, say, $200^\circ	ext{C}$ on the upper part and $10^\circ	ext{C}$ on the lower part.

## 18.4 Fluid Flow {#sec-fluid-flow}

Laplace’s equation also plays a basic role in hydrodynamics, in steady nonviscous fluid flow under physical conditions discussed later in this section. For methods of complex analysis to be applicable, our problems will be two-dimensional, so that the velocity vector $V$ by which the motion of the fluid can be given depends only on two space variables $x$ and $y$, and the motion is the same in all planes parallel to the $xy$-plane.

Then we can use for the velocity vector $V$ a complex function

$$V = V_1 + iV_2$$ {#eq-sec184-1}

giving the magnitude and direction of the velocity at each point $z = x + iy$. Here $V_1$ and $V_2$ are the components of the velocity in the $x$ and $y$ directions. $V$ is tangential to the path of the moving particles, called a **streamline** of the motion (Fig. 414).

![Fig. 414. Velocity](../images/fig-18-19.png){#fig-18-19}

We show that under suitable assumptions (explained in detail following the examples), for a given flow there exists an analytic function

$$F(z) = \Phi(x, y) + i\Psi(x, y)$$ {#eq-sec184-2}

called the **complex potential** of the flow, such that the streamlines are given by $\Psi(x, y) = 	ext{const}$, and the velocity vector (or, briefly, the velocity) is given by

$$V = V_1 + iV_2 = \overline{F'(z)}$$ {#eq-sec184-3}

where the bar denotes the complex conjugate. $\Psi$ is called the **stream function**. The function $\Phi$ is called the **velocity potential**. The curves $\Phi(x, y) = 	ext{const}$ are called **equipotential lines**. The velocity vector $V$ is the gradient of $\Phi$; by definition, this means that

$$V_1 = rac{\partial \Phi}{\partial x}, \quad V_2 = rac{\partial \Phi}{\partial y}$$ {#eq-sec184-4}

Indeed, for $F = \Phi + i\Psi$ Eq. (4) in Sec. 13.4 is $F'(z) = \Phi_x + i\Psi_x = \Phi_x - i\Phi_y$ by the second Cauchy–Riemann equation. Together we obtain (3):

$$\overline{F'(z)} = \Phi_x + i\Phi_y = V_1 + iV_2 = V$$

Furthermore, since $F(z)$ is analytic, $\Phi$ and $\Psi$ satisfy Laplace’s equation

$$
abla^2 \Phi = 0, \quad 
abla^2 \Psi = 0$$ {#eq-sec184-5}

Whereas in electrostatics the boundaries (conducting plates) are equipotential lines, in fluid flow the boundaries across which fluid cannot flow must be streamlines. Hence in fluid flow the stream function $\Psi$ is of particular importance.

Before discussing the conditions for the validity of the statements involving (2)–(5), let us consider two flows of practical interest, so that we first see what is going on from a practical point of view. Further flows are included in the problem set.

### EXAMPLE 1 Flow Around a Corner
The complex potential $F(z) = z^2$ models a flow with
- Equipotential lines (Hyperbolas): $\Phi = x^2 - y^2 = 	ext{const}$
- Streamlines (Hyperbolas): $\Psi = 2xy = 	ext{const}$.

From (3) we obtain the velocity vector $V = \overline{F'(z)} = 2\overline{z} = 2(x - iy)$, that is,

$$V_1 = 2x, \quad V_2 = -2y$$

The speed (magnitude of the velocity) is

$$|V| = \sqrt{V_1^2 + V_2^2} = 2\sqrt{x^2 + y^2} = 2|z|$$

The flow may be interpreted as the flow in a channel bounded by the positive coordinate axes and a hyperbola, say, $xy = 1$ (Fig. 415). We note that the speed along a streamline $S$ has a minimum at the point $P$ where the cross section of the channel is large.

![Fig. 415. Flow around a corner (Example 1)](../images/fig-18-20.png){#fig-18-20}

### EXAMPLE 2 Flow Around a Cylinder
Consider the complex potential

$$F(z) = z + rac{1}{z}$$

Using the polar form $z = re^{i	heta}$ we obtain

$$F(z) = re^{i	heta} + rac{1}{r}e^{-i	heta} = \left(r + rac{1}{r}ight)\cos	heta + i\left(r - rac{1}{r}ight)\sin	heta$$

Hence the streamlines are

$$\Psi(x, y) = \left(r - rac{1}{r}ight)\sin	heta = 	ext{const}$$

In particular, $\Psi(x, y) = 0$ gives $r - 1/r = 0$ or $\sin	heta = 0$. Hence this streamline consists of the unit circle $r = 1$ and the $x$-axis ($	heta = 0$ and $	heta = \pi$). For large $|z|$ the term $1/z$ in $F(z)$ is small in absolute value, so that for these $z$ the flow is nearly uniform and parallel to the $x$-axis. Hence we can interpret this as a flow around a long circular cylinder of unit radius that is perpendicular to the $z$-plane and intersects it in the unit circle $|z| = 1$ and whose axis corresponds to $z = 0$.

The flow has two stagnation points (that is, points at which the velocity $V$ is zero), at $z = \pm 1$. This follows from (3) and

$$F'(z) = 1 - rac{1}{z^2} = rac{z^2 - 1}{z^2}$$

hence $z^2 - 1 = 0$. (See Fig. 416.)

![Fig. 416. Flow around a cylinder (Example 2)](../images/fig-18-21.png){#fig-18-21}

![Fig. 417. Tangential component of the velocity with respect to a curve C](../images/fig-18-22.png){#fig-18-22}

### Assumptions and Theory Underlying (2)–(5)

### THEOREM 1 Complex Potential of a Flow
If the domain of flow is simply connected and the flow is irrotational and incompressible, then the statements involving (2)–(5) hold. In particular, then the flow has a complex potential $F(z)$ which is an analytic function.

**PROOF** We prove this theorem, along with a discussion of basic concepts related to fluid flow.

**(a) First Assumption: Irrotational.** Let $C$ be any smooth curve in the $z$-plane given by $z(s) = x(s) + iy(s)$, where $s$ is the arc length of $C$. Let the real variable $V_t$ be the component of the velocity $V$ tangent to $C$ (Fig. 417). Then the value of the real line integral

$$\int_C V_t \, ds$$ {#eq-sec184-6}

taken along $C$ in the sense of increasing $s$ is called the **circulation** of the fluid along $C$. Dividing the circulation by the length of $C$, we obtain the mean velocity of the flow along the curve $C$. (See footnote 1.)

Now $V_t = |V| \cos lpha$ (Fig. 417). Hence $V_t \, ds$ is the dot product of $V$ and the tangent vector of $C$; thus in (6),

$$V_t \, ds = \left(V_1 rac{dx}{ds} + V_2 rac{dy}{ds}ight) ds = V_1 \, dx + V_2 \, dy$$

The circulation (6) along $C$ now becomes

$$\int_C V_t \, ds = \int_C (V_1 \, dx + V_2 \, dy)$$ {#eq-sec184-7}

As the next idea, let $C$ be a closed curve and let $C$ be the boundary of a simply connected domain $D$. Suppose further that $V$ has continuous partial derivatives in a domain containing $D$ and $C$. Then we can use Green’s theorem (Sec. 10.4) to represent the circulation around $C$ by a double integral:

$$\oint_C (V_1 \, dx + V_2 \, dy) = \iint_D \left(rac{\partial V_2}{\partial x} - rac{\partial V_1}{\partial y}ight) dx \, dy$$ {#eq-sec184-8}

The integrand of this double integral is called the **vorticity** of the flow. The vorticity divided by 2 is called the **rotation**

$$\omega(x, y) = rac{1}{2}\left(rac{\partial V_2}{\partial x} - rac{\partial V_1}{\partial y}ight)$$ {#eq-sec184-9}

We assume the flow to be irrotational, that is, $\omega(x, y) = 0$ throughout the flow; thus,

$$rac{\partial V_2}{\partial x} - rac{\partial V_1}{\partial y} = 0$$ {#eq-sec184-10}

To understand the physical meaning of vorticity and rotation, take for $C$ in (8) a circle. Let $r$ be the radius of $C$. Then the circulation divided by the length of $C$ is the mean velocity of the fluid along $C$. Hence by dividing this by $r$ we obtain the mean angular velocity $\omega_0$ of the fluid about the center of the circle:

$$\omega_0 = rac{1}{2\pi r^2} \iint_D \left(rac{\partial V_2}{\partial x} - rac{\partial V_1}{\partial y}ight) dx \, dy = rac{1}{\pi r^2} \iint_D \omega(x, y) \, dx \, dy$$

If we now let $r 	o 0$, the limit of $\omega_0$ is the value of $\omega$ at the center of $C$. Hence $\omega(x, y)$ is the limiting angular velocity of a circular element of the fluid as the circle shrinks to the point $(x, y)$. Roughly speaking, if a spherical element of the fluid were suddenly solidified and the surrounding fluid simultaneously annihilated, the element would rotate with the angular velocity $\omega(x, y)$.

> **^1^ Definitions:**  
> - Mean value of $f$ on the interval $[a, b]$: $rac{1}{b-a} \int_a^b f(x) \, dx$  
> - Mean value of $f$ on $C$ (length $L$): $rac{1}{L} \int_C f(s) \, ds$  
> - Mean value of $f$ on $D$ (area $A$): $rac{1}{A} \iint_D f(x, y) \, dx \, dy$

**(b) Second Assumption: Incompressible.** Our second assumption is that the fluid is incompressible. (Fluids include liquids, which are incompressible, and gases, such as air, which are compressible.) Then

$$rac{\partial V_1}{\partial x} + rac{\partial V_2}{\partial y} = 0$$ {#eq-sec184-11}

in every region that is free of sources or sinks, that is, points at which fluid is produced or disappears, respectively. The expression in (11) is called the **divergence** of $V$ and is denoted by $	ext{div } V$. (See also (7) in Sec. 9.8.)

**(c) Complex Velocity Potential.** If the domain $D$ of the flow is simply connected (Sec. 14.2) and the flow is irrotational, then (10) implies that the line integral (7) is independent of path in $D$ (by Theorem 3 in Sec. 10.2). Hence if we integrate from a fixed point $(a, b)$ in $D$ to a variable point $(x, y)$ in $D$, the integral becomes a function of the point $(x, y)$, say, $\Phi(x, y)$:

$$\Phi(x, y) = \int_{(a, b)}^{(x, y)} (V_1 \, dx + V_2 \, dy)$$ {#eq-sec184-12}

We claim that the flow has a velocity potential which is given by (12). To prove this, all we have to do is to show that (4) holds. Now since the integral (7) is independent of path, $V_1 \, dx + V_2 \, dy$ is exact, namely, the differential of $\Phi$, that is,

$$V_1 \, dx + V_2 \, dy = rac{\partial \Phi}{\partial x} \, dx + rac{\partial \Phi}{\partial y} \, dy$$

From this we see that $V_1 = \partial\Phi/\partial x$ and $V_2 = \partial\Phi/\partial y$, which gives (4). That $\Phi$ is harmonic follows at once by substituting (4) into (11), which gives the first Laplace equation in (5).

We finally take a harmonic conjugate $\Psi(x, y)$ of $\Phi(x, y)$. Then the other equation in (5) holds. Also, since the second partial derivatives of $\Phi$ and $\Psi$ are continuous, we see that the complex function $F(z) = \Phi(x, y) + i\Psi(x, y)$ is analytic in $D$. Since the curves $\Psi(x, y) = 	ext{const}$ are perpendicular to the equipotential curves $\Phi(x, y) = 	ext{const}$ (except where $F'(z) = 0$), we conclude that the curves $\Psi = 	ext{const}$ are the streamlines. Hence $\Psi$ is the stream function and $F(z)$ is the complex potential of the flow. This completes the proof of Theorem 1. $lacksquare$

### PROBLEM SET 18.4 {#sec-ps18-4}

1. **Differentiability.** Under what condition on the velocity vector $V$ in (1) will $F(z)$ in (2) be analytic?
2. **Corner flow.** Along what curves will the speed in Example 1 be constant? Is this obvious from Fig. 415?
3. **Cylinder.** Guess from physics and from Fig. 416 where on the $y$-axis the speed is maximum. Then calculate.
4. **Cylinder.** Calculate the speed along the cylinder wall in Fig. 416, also confirming the answer to Prob. 3.
5. **Irrotational flow.** Show that the flow in Example 2 is irrotational.
6. **Extension of Example 1.** Sketch or graph and interpret the flow in Example 1 on the whole upper half-plane.
7. **Parallel flow.** Sketch and interpret the flow with complex potential $F(z) = z$.
8. **Parallel flow.** What is the complex potential of an upward parallel flow of speed $v_0$ in the direction of $y = x$? Sketch the flow.
9. **Corner.** What would be a suitable complex potential $F(z)$ in Example 1 if the angle of the corner were $60^\circ$ ($\pi/3$ rad) instead of $90^\circ$?
10. **Corner.** Show that $F(z) = iz^2$ also models a flow around a corner. Sketch streamlines and equipotential lines. Find $V$.
11. What flow do you obtain from $F(z) = z$ with $K$ positive real?
12. **Conformal mapping.** Obtain the flow in Example 1 from that in Prob. 11 by a suitable conformal mapping.
13. **Sector.** What would be a suitable $F(z)$ in Example 1 if the angle at the corner were $45^\circ$?
14. Sketch or graph streamlines and equipotential lines of $F(z) = iz^3$. Find $V$. Find all points at which $V$ is horizontal.
15. Change $F(z)$ in Example 2 slightly to obtain a flow around a cylinder of radius $R_0$ that gives the flow in Example 2 if $R_0 = 1$.
16. **Cylinder.** What happens in Example 2 if you replace $z$ by $z^2$? Sketch and interpret the resulting flow in the first quadrant.
17. **Elliptic cylinder.** Show that $F(z) = 	ext{arccos } z$ gives confocal ellipses as streamlines, with foci at $z = \pm 1$, and that the flow circulates around an elliptic cylinder or a plate (the segment from $-1$ to 1 in Fig. 418).
    
    ![Fig. 418. Flow around a plate in Prob. 17](../images/fig-18-23.png){#fig-18-23}
18. **Aperture.** Show that $F(z) = 	ext{arccosh } z$ gives confocal hyperbolas as streamlines, with foci at $z = \pm 1$; the flow may be interpreted as a flow through an aperture (Fig. 419).

    ![Fig. 419. Flow through an aperture in Prob. 18](../images/fig-18-24.png){#fig-18-24}

19. **Potential.** Show that the streamlines of $F(z) = 1/z$ are circles through the origin with centers on the $y$-axis.
20. **TEAM PROJECT. Role of the Natural Logarithm in Modeling Flows.**
    (a) **Basic flows: Source and sink.** Show that $F(z) = (Q/2\pi) 	ext{Ln } z$ with constant positive real $Q$ gives a flow directed radially outward (Fig. 420), so that $F$ models a point source at $z = 0$ (that is, a source line in space) at which fluid is produced. $Q$ is called the strength or discharge of the source. If $Q$ is negative real, show that the flow is directed radially inward, so that $F$ models a sink at a point at which fluid disappears. Note that $z = 0$ is the singular point of $F(z)$.
    
    ![Fig. 420. Point source](../images/fig-18-25.png){#fig-18-25}
    
    (b) **Basic flows: Vortex.** Show that $F(z) = -(i\Gamma/2\pi)	ext{Ln } z$ with positive real $\Gamma$ gives a flow circulating counterclockwise around $z = 0$ (Fig. 421). This is called a vortex. Note that each time we travel around the vortex, the potential increases by $\Gamma$.
    
    ![Fig. 421. Vortex flow](../images/fig-18-26.png){#fig-18-26}
    
    (c) **Addition of flows.** Show that addition of the velocity vectors of two flows gives a flow whose complex potential is obtained by adding the complex potentials of those flows.
    (d) **Source and sink combined.** Find the complex potentials of a flow with a source of strength $1$ at $z = -a$ and of a flow with a sink of strength $1$ at $z = a$. Add both and sketch or graph the streamlines. Show that for small $|a|$ these lines look similar to those in Prob. 19.
    (e) **Flow with circulation around a cylinder.** Add the potential in (b) to that in Example 2. Show that this gives a flow for which the cylinder wall $|z| = 1$ is a streamline. Find the speed and show that the stagnation points are $z = i\left( rac{\Gamma}{4\pi} \pm \sqrt{1 - rac{\Gamma^2}{16\pi^2}} ight)$. (See Fig. 422.)
    
    ![Fig. 422. Flow around a cylinder with circulation](../images/fig-18-27.png){#fig-18-27}

## 18.5 Poisson's Integral Formula for Potentials {#sec-poisson-formula}

So far in this chapter we have seen powerful methods based on conformal mappings and complex potentials. They were used for modeling and solving two-dimensional potential problems and demonstrated the importance of complex analysis.

Now we introduce a further method that results from complex integration. It will yield the very important Poisson integral formula (5) for potentials in a standard domain (a circular disk). In addition, from (5), we will derive a useful series (7) for these potentials. This allows us to solve problems for disks and then map solutions conformally onto other domains.

### Derivation of Poisson's Integral Formula
Poisson's formula will follow from Cauchy's integral formula (Sec. 14.3)

$$F(z) = rac{1}{2\pi i} \oint_C rac{F(z^*)}{z^* - z} dz^*$$ {#eq-sec185-1}

Here $C$ is the circle $z^* = Re^{ilpha}$ (counterclockwise), and we assume that $F(z)$ is analytic in a domain containing $C$ and its full interior. Since $dz^* = iRe^{ilpha} dlpha = iz^* dlpha$, we obtain from (1)

$$F(z) = rac{1}{2\pi} \int_0^{2\pi} F(z^*) rac{z^*}{z^* - z} dlpha$$ {#eq-sec185-2}

Now comes a little trick. If instead of $z$ inside $C$ we take a point $Z$ outside $C$, the integrals (1) and (2) are zero by Cauchy’s integral theorem (Sec. 14.2). We choose $Z = z^* \overline{z^*} / \overline{z} = R^2 / \overline{z}$, which is outside $C$ because $|Z| = R^2 / |z| > R$ (since $|z| = r < R$). From (2) we thus have

$$0 = rac{1}{2\pi} \int_0^{2\pi} F(z^*) rac{z^*}{z^* - Z} dlpha = rac{1}{2\pi} \int_0^{2\pi} F(z^*) rac{z^*}{z^* - R^2/\overline{z}} dlpha$$

and by straightforward simplification using $R^2 = z^* \overline{z^*}$,

$$0 = rac{1}{2\pi} \int_0^{2\pi} F(z^*) rac{\overline{z}}{\overline{z} - \overline{z^*}} dlpha$$

We subtract this from (2) and use the following formula that you can verify by direct calculation ($z^*\overline{z^*}$ cancels):

$$rac{z^*}{z^* - z} - rac{\overline{z}}{\overline{z} - \overline{z^*}} = rac{z^*\overline{z^*} - z\overline{z}}{(z^* - z)(\overline{z^*} - \overline{z})}$$ {#eq-sec185-3}

We then have

$$F(z) = rac{1}{2\pi} \int_0^{2\pi} F(z^*) rac{z^*\overline{z^*} - z\overline{z}}{(z^* - z)(\overline{z^*} - \overline{z})} dlpha$$ {#eq-sec185-4}

From the polar representations $z^* = Re^{ilpha}$ and $z = re^{i	heta}$ we see that the quotient in the integrand is real and equal to

$$rac{R^2 - r^2}{(Re^{ilpha} - re^{i	heta})(Re^{-ilpha} - re^{-i	heta})} = rac{R^2 - r^2}{R^2 - 2Rr\cos(	heta - a) + r^2}$$

We now write $F(z) = \Phi(r, 	heta) + i\Psi(r, 	heta)$ and take the real part on both sides of (4). Then we obtain **Poisson’s integral formula**:

$$\Phi(r, 	heta) = rac{1}{2\pi} \int_0^{2\pi} \Phi(R, a) rac{R^2 - r^2}{R^2 - 2Rr\cos(	heta - a) + r^2} dlpha$$ {#eq-sec185-5}

This formula represents the harmonic function $\Phi$ in the disk $|z| < R$ in terms of its values on the boundary (the circle) $|z| = R$.

Formula (5) is still valid if the boundary function $\Phi(R, a)$ is merely piecewise continuous (as is practically often the case; see Figs. 405 and 406 in Sec. 18.2 for an example). Then (5) gives a function harmonic in the open disk, and on the circle equal to the given boundary function, except at points where the latter is discontinuous. A proof can be found in Ref. [D1] in App. 1.

### Series for Potentials in Disks
From (5) we may obtain an important series development of $\Phi$ in terms of simple harmonic functions. We remember that the quotient in the integrand of (5) was derived from (3). We claim that the right side of (3) is the real part of

$$rac{z^* + z}{z^* - z}$$

Indeed, the last denominator is real and so is $z^*\overline{z^*} - z\overline{z}$ in the numerator, whereas $2i	ext{Im}(z^*\overline{z})$ in the numerator is pure imaginary. This verifies our claim.

Now by the use of the geometric series we obtain (develop the denominator in terms of $z/z^*$):

$$rac{z^* + z}{z^* - z} = rac{1 + z/z^*}{1 - z/z^*} = \left(1 + rac{z}{z^*}ight) \sum_{n=0}^\infty \left(rac{z}{z^*}ight)^n = 1 + 2 \sum_{n=1}^\infty \left(rac{z}{z^*}ight)^n$$ {#eq-sec185-6}

Since $z^* = Re^{ilpha}$ and $z = re^{i	heta}$, we have

$$\left(rac{z}{z^*}ight)^n = \left(rac{r}{R}ight)^n e^{in(	heta - a)}$$

On the right, $e^{in(	heta - a)} = \cos n(	heta - a) + i\sin n(	heta - a)$. Hence from (6) we obtain

$$	ext{Re}\left( rac{z^* + z}{z^* - z} ight) = 1 + 2 \sum_{n=1}^\infty \left(rac{r}{R}ight)^n \cos n(	heta - a)$$ {#eq-sec185-6star}

This expression is equal to the quotient in (5), as we have mentioned before, and by inserting it into (5) and integrating term by term with respect to $lpha$ from 0 to $2\pi$, we obtain

$$\Phi(r, 	heta) = a_0 + \sum_{n=1}^\infty \left(rac{r}{R}ight)^n (a_n \cos n	heta + b_n \sin n	heta)$$ {#eq-sec185-7}

where the coefficients are [the 2 in (6*) cancels the $1/(2\pi)$ in (5)]:

$$a_0 = rac{1}{2\pi}\int_0^{2\pi} \Phi(R, a) \, dlpha, \quad a_n = rac{1}{\pi}\int_0^{2\pi} \Phi(R, a)\cos na \, dlpha, \quad b_n = rac{1}{\pi}\int_0^{2\pi} \Phi(R, a)\sin na \, dlpha$$ {#eq-sec185-8}

the Fourier coefficients of $\Phi(R, a)$. Now, for $r = R$, the series (7) becomes the Fourier series of $\Phi(R, a)$. Hence the representation (7) will be valid whenever the $\Phi(R, a)$ given on the boundary can be represented by a Fourier series.

### EXAMPLE 1 Dirichlet Problem for the Unit Disk
Find the electrostatic potential $\Phi(r, 	heta)$ in the unit disk $r < 1$ having the boundary values (Fig. 423)

$$\Phi(1, a) = egin{cases} -a/\pi & 	ext{if } -\pi < a < 0 \ a/\pi & 	ext{if } 0 < a < \pi \end{cases}$$

![Fig. 423. Boundary values in Example 1](../images/fig-18-28.png){#fig-18-28}

![Fig. 424. Potential in Example 1](../images/fig-18-29.png){#fig-18-29}

**Solution.** Since $\Phi(1, a)$ is even, $b_n = 0$, and from (8) we obtain $a_0 = 1/2$ and

$$a_n = rac{1}{\pi} \left[ \int_{-\pi}^0 \left(-rac{a}{\pi}ight) \cos na \, dlpha + \int_0^\pi rac{a}{\pi} \cos na \, dlpha ight] = rac{2}{n^2\pi^2}(\cos n\pi - 1)$$

Hence, $a_n = 0$ if $n$ is even, $a_n = -4/(n^2\pi^2)$ if $n$ is odd, and the potential is

$$\Phi(r, 	heta) = rac{1}{2} - rac{4}{\pi^2}\left( r\cos	heta + rac{r^3}{3^2}\cos 3	heta + rac{r^5}{5^2}\cos 5	heta + \dots ight)$$

Figure 424 shows the unit disk and some of the equipotential lines ($\Phi = 	ext{const}$).

### PROBLEM SET 18.5 {#sec-ps18-5}

1. Give the details of the derivation of the series (7) from the Poisson formula (5).
2. Verify formula (3).
3. Show that each term of (7) is a harmonic function in the disk $r < R$.
4. Why does the series in Example 1 reduce to a cosine series?

#### 5–18 HARMONIC FUNCTIONS IN A DISK
Using (7), find the potential in the unit disk $r < 1$ having the given boundary values $\Phi(1, 	heta)$. Using the sum of the first few terms of the series, compute some values of $\Phi$ and sketch a figure of the equipotential lines.
5. $\Phi(1, 	heta) = 3 \sin 3	heta$.
6. $\Phi(1, 	heta) = 5 - \cos 2	heta$.
7. $\Phi(1, 	heta) = \cos^2 4	heta$.
8. $\Phi(1, 	heta) = 4\sin^3	heta$.
9. $\Phi(1, 	heta) = 8\sin^4	heta$.
10. $\Phi(1, 	heta) = 16\cos^3 2	heta$.
11. $\Phi(1, 	heta) = 	heta/\pi$ if $-\pi < 	heta < \pi$.
12. $\Phi(1, 	heta) = k$ if $0 < 	heta < \pi$, and 0 otherwise.
13. $\Phi(1, 	heta) = 	heta$ if $-\pi/2 < 	heta < \pi/2$, and 0 otherwise.
14. $\Phi(1, 	heta) = |	heta|/\pi$ if $-\pi < 	heta < \pi$.
15. $\Phi(1, 	heta) = 1$ if $-\pi/2 < 	heta < \pi/2$, and 0 otherwise.
16. $\Phi(1, 	heta) = 0$ if $-\pi < 	heta < 0$, and $	heta$ if $0 < 	heta < \pi$.
17. $\Phi(1, 	heta) = 	heta^2/\pi^2$ if $-\pi < 	heta < \pi$.
18. $\Phi(1, 	heta) = 	heta + \pi$ if $-\pi < 	heta < 0$, and $	heta - \pi$ if $0 < 	heta < \pi$.

#### 19–20 TEAM PROJECTS
19. **CAS EXPERIMENT. Series (7).** Write a program for series developments (7). Experiment on accuracy by computing values from partial sums and comparing them with values that you obtain from your CAS graph. Do this (a) for Example 1 and Fig. 424, (b) for $\Phi(1, 	heta)$ in Prob. 12 (which is discontinuous on the boundary!), (c) for a $\Phi(1, 	heta)$ of your choice with continuous boundary values, and (d) for a $\Phi(1, 	heta)$ with discontinuous boundary values.
20. **TEAM PROJECT. Potential in a Disk.**
    (a) **Mean value property.** Show that the value of a harmonic function at the center of a circle $C$ equals the mean of the values of $\Phi$ on $C$.
    (b) **Separation of variables.** Show that the terms of (7) appear as solutions in separating the Laplace equation in polar coordinates.
    (c) **Harmonic conjugate.** Find a series for a harmonic conjugate of $\Phi$ from (7). Hint. Use the Cauchy–Riemann equations.
    (d) **Power series.** Find a series for $F(z) = \Phi + i\Psi$ from (7).

## 18.6 General Properties of Harmonic Functions. Uniqueness Theorem for the Dirichlet Problem {#sec-harmonic-properties}

Recall from Sec. 10.8 that harmonic functions are solutions to Laplace’s equation and their second-order partial derivatives are continuous. In this section we explore how general properties of harmonic functions often can be obtained from properties of analytic functions. This can frequently be done in a simple fashion. Specifically, important mean value properties of harmonic functions follow readily from those of analytic functions. The details are as follows.

### THEOREM 1 Mean Value Property of Analytic Functions
Let $F(z)$ be analytic in a simply connected domain $D$. Then the value of $F(z)$ at a point $z_0$ in $D$ is equal to the mean value of $F(z)$ on any circle in $D$ with center at $z_0$.

**PROOF** In Cauchy’s integral formula (Sec. 14.3)

$$F(z_0) = rac{1}{2\pi i} \oint_C rac{F(z)}{z - z_0} dz$$ {#eq-sec186-1}

we choose for $C$ the circle $z = z_0 + re^{ilpha}$ in $D$. Then $z - z_0 = re^{ilpha}$ and $dz = ire^{ilpha} dlpha$, and (1) becomes

$$F(z_0) = rac{1}{2\pi} \int_0^{2\pi} F(z_0 + re^{ilpha}) dlpha$$ {#eq-sec186-2}

The right side is the mean value of $F(z)$ on the circle. This proves the theorem. $lacksquare$

For harmonic functions, Theorem 1 implies

### THEOREM 2 Two Mean Value Properties of Harmonic Functions
Let $\Phi(x, y)$ be harmonic in a simply connected domain $D$. Then the value of $\Phi(x, y)$ at a point $(x_0, y_0)$ in $D$ is equal to the mean value of $\Phi(x, y)$ on any circle in $D$ with center at $(x_0, y_0)$. This value is also equal to the mean value of $\Phi(x, y)$ on any circular disk in $D$ with center $(x_0, y_0)$.

**PROOF** The first part of the theorem follows from (2) by taking the real parts on both sides,

$$\Phi(x_0, y_0) = 	ext{Re } F(z_0) = rac{1}{2\pi} \int_0^{2\pi} \Phi(x_0 + r\coslpha, y_0 + r\sinlpha) dlpha$$

The second part of the theorem follows by integrating this formula over $r$ from 0 to $r_0$ (the radius of the disk) and dividing by $r_0^2/2$:

$$\Phi(x_0, y_0) = rac{1}{\pi r_0^2} \int_0^{r_0} \int_0^{2\pi} \Phi(x_0 + r\coslpha, y_0 + r\sinlpha) r \, dlpha \, dr$$ {#eq-sec186-3}

The right side is the indicated mean value (integral divided by the area of the region of integration). $lacksquare$

Returning to analytic functions, we state and prove another famous consequence of Cauchy’s integral formula. The proof is indirect and shows quite a nice idea of applying the $ML$-inequality. (A bounded region is a region that lies entirely in some circle about the origin.)

### THEOREM 3 Maximum Modulus Theorem for Analytic Functions
Let $F(z)$ be analytic and nonconstant in a domain containing a bounded region $R$ and its boundary. Then the absolute value $|F(z)|$ cannot have a maximum at an interior point of $R$. Consequently, the maximum of $|F(z)|$ is taken on the boundary of $R$. If $F(z) 
eq 0$ in $R$, the same is true with respect to the minimum of $|F(z)|$.

![Fig. 425. Proof of Theorem 3](../images/fig-18-30.png){#fig-18-30}

**PROOF** We assume that $|F(z)|$ has a maximum at an interior point $z_0$ of $R$ and show that this leads to a contradiction. Let $|F(z_0)| = M$ be this maximum. Since $F(z)$ is not constant, $|F(z)|$ is not constant, as follows from Example 3 in Sec. 13.4. Consequently, we can find a circle $C$ of radius $r$ with center at $z_0$ such that the interior of $C$ is in $R$ and $|F(z)|$ is smaller than $M$ at some point $P$ of $C$. Since $|F(z)|$ is continuous, it will be smaller than $M$ on an arc $C_1$ of $C$ that contains $P$ (see Fig. 425), say,

$$|F(z)| \le M - k \quad (k > 0)$$

for all $z$ on $C_1$. Let $C_1$ have the length $L_1$. Then the complementary arc $C_2$ of $C$ has the length $2\pi r - L_1$. We now apply the $ML$-inequality (Sec. 14.1) to (1) and note that $|z - z_0| = r$. We then obtain (using straightforward calculation in the second line of the formula)

$$egin{aligned}
M = |F(z_0)| &\le rac{1}{2\pi} \left| \int_{C_1} rac{F(z)}{z - z_0} dz ight| + rac{1}{2\pi} \left| \int_{C_2} rac{F(z)}{z - z_0} dz ight| \
&\le rac{1}{2\pi} (M - k) rac{L_1}{r} + rac{1}{2\pi} M rac{2\pi r - L_1}{r} = M - rac{k L_1}{2\pi r} < M
\end{aligned}$$

that is, $M < M$, which is impossible. Hence our assumption is false and the first statement is proved.

Next we prove the second statement. If $F(z) 
eq 0$ in $R$, then $1/F(z)$ is analytic in $R$. From the statement already proved it follows that the maximum of $1/|F(z)|$ lies on the boundary of $R$. But this maximum corresponds to the minimum of $|F(z)|$. This completes the proof. $lacksquare$

This theorem has several fundamental consequences for harmonic functions, as follows.

### THEOREM 4 Harmonic Functions
Let $\Phi(x, y)$ be harmonic in a domain containing a simply connected bounded region $R$ and its boundary curve $C$. Then:
- **(I) (Maximum principle)** If $\Phi(x, y)$ is not constant, it has neither a maximum nor a minimum in $R$. Consequently, the maximum and the minimum are taken on the boundary of $R$.
- **(II)** If $\Phi(x, y)$ is constant on $C$, then $\Phi(x, y)$ is a constant.
- **(III)** If $h(x, y)$ and $\Phi(x, y)$ are harmonic in $R$ and on $C$, and if $h(x, y) = \Phi(x, y)$ on $C$, then $h(x, y) = \Phi(x, y)$ everywhere in $R$.

**PROOF** 
**(I)** Let $\Psi(x, y)$ be a conjugate harmonic function of $\Phi(x, y)$ in $R$. Then the complex function $F(z) = \Phi(x, y) + i\Psi(x, y)$ is analytic in $R$, and so is $G(z) = e^{F(z)}$. Its absolute value is

$$|G(z)| = e^{	ext{Re } F(z)} = e^{\Phi(x, y)}$$

From Theorem 3 it follows that $|G(z)|$ cannot have a maximum at an interior point of $R$. Since $e^\Phi$ is a monotone increasing function of the real variable $\Phi$, the statement about the maximum of $\Phi$ follows. From this, the statement about the minimum follows by replacing $\Phi$ by $-\Phi$.

**(II)** By (I) the function $\Phi$ takes its maximum and its minimum on $C$. Thus, if $\Phi$ is constant on $C$, its minimum must equal its maximum, so that $\Phi$ must be a constant.

**(III)** If $h$ and $\Phi$ are harmonic in $R$ and on $C$, then $h - \Phi$ is also harmonic in $R$ and on $C$, and by assumption, $h - \Phi = 0$ everywhere on $C$. By (II) we thus have $h - \Phi = 0$ everywhere in $R$, and (III) is proved. $lacksquare$

The last statement of Theorem 4 is very important. It means that a harmonic function is uniquely determined in $R$ by its values on the boundary of $R$. Usually, $\Phi(x, y)$ is required to be harmonic in $R$ and continuous on the boundary of $R$, that is,

$$\lim_{(x, y) 	o (x_0, y_0)} \Phi(x, y) = \Phi(x_0, y_0)$$

where $(x_0, y_0)$ is on the boundary and $(x, y)$ is in $R$. Under these assumptions the maximum principle (I) is still applicable. The problem of determining $\Phi$ when the boundary values are given is called the Dirichlet problem for the Laplace equation in two variables, as we know. From (III) we thus have, as a highlight of our discussion:

### THEOREM 5 Uniqueness Theorem for the Dirichlet Problem
If for a given region and given boundary values the Dirichlet problem for the Laplace equation in two variables has a solution, the solution is unique.

### PROBLEM SET 18.6 {#sec-ps18-6}

#### 1–4 APPLICATION OF THEOREM 1
Verify Theorem 1 for the given $F(z)$ and circle of radius 1 with center $z_0$.
1. $F(z) = (z + 1)^3$, $z_0 = 5$.
2. $F(z) = 2z^4$, $z_0 = -2$.
3. $F(z) = (3z - 2)^2$, $z_0 = 4$.
4. $F(z) = (z - 1)^{-2}$, $z_0 = -1$.
5. Integrate $|z|$ around the unit circle. Does the result contradict Theorem 1?
6. Derive the first statement in Theorem 2 from Poisson’s integral formula.

#### 7–9 DISK MEAN VALUE
Verify formula (3) in Theorem 2 for the given $\Phi(x, y)$ and circle of radius 1 with center $(x_0, y_0)$.
7. $\Phi(x, y) = x^2 - y^2$, $(x_0, y_0) = (1, 1)$.
8. $\Phi(x, y) = (x - 1)(y - 1)$, $(x_0, y_0) = (3, 8)$.
9. $\Phi(x, y) = x + y + xy$, $(x_0, y_0) = (2, -2)$.
10. Verify the calculations involving the inequalities in the proof of Theorem 3.
11. **CAS EXPERIMENT. Graphing Potentials.** Graph the potentials in Probs. 7 and 9 and for two other functions of your choice as surfaces over a rectangle in the $xy$-plane. Find the locations of the maxima and minima by inspecting these graphs.
12. **TEAM PROJECT. Maximum Modulus of Analytic Functions.**
    (a) Verify Theorem 3 for (i) $F(z) = z^2$ and the rectangle $1 \le x \le 5$, $2 \le y \le 4$, (ii) $F(z) = \sin z$ and the unit disk, and (iii) $F(z) = e^z$ and any bounded domain.
    (b) $F(z) = 1/(1+|z|)$ is not zero in the disk $|z| \le 2$ and has a minimum at an interior point. Does this contradict Theorem 3?
    (c) $F(x) = \sin x$ ($x$ real) has a maximum 1 at $x = \pi/2$. Why can this not be a maximum of $|F(z)|$ in a domain containing $z = \pi/2$?
    (d) If $F(z)$ is analytic and not constant in the closed unit disk $D: |z| \le 1$ and $|F(z)| = c = 	ext{const}$ on the unit circle, show that $F(z)$ must have a zero in $D$.

#### 13–17 MAXIMUM MODULUS
Find the location and size of the maximum of $|F(z)|$ in the unit disk $|z| \le 1$.
13. $F(z) = \cos z$.
14. $F(z) = \sinh 2z$.
15. $F(z) = \exp z^2$.
16. $F(z) = az + b$ ($a, b$ complex, $a 
eq 0$).
17. $F(z) = \cosh z$.
18. Verify the maximum principle for $\Phi(x, y) = e^x \sin y$ and the rectangle $0 \le x \le 1, 0 \le y \le 2\pi$.
19. **Harmonic conjugate.** Do $\Phi(x, y)$ and a harmonic conjugate $\Psi(x, y)$ in a region $R$ have their maximum at the same point of $R$?
20. **Conformal mapping.** Find the location of the maximum of $|F(z)|$ in $R: |z| \le 1, y \ge 0$, where $F(z) = z^2$. Find the region $R^*$ in the $w$-plane that is mapped onto $R$ by $w = f(z) = z^2$. Find the potential $\Phi$ in $R$ resulting from $\Phi^* = e^u \cos v$ and the location of the maximum. Is the image of $(x_1, y_1)$ equal to $(u_1, v_1)$? If so, is this just by chance?

## CHAPTER 18 REVIEW QUESTIONS AND PROBLEMS {#sec-ch18-review}

1. Why can potential problems be modeled and solved by methods of complex analysis? For what dimensions?
2. What parts of complex analysis are mainly of interest to the engineer and physicist?
3. What is a harmonic function? A harmonic conjugate?
4. What areas of physics did we consider? Could you think of others?
5. Give some examples of potential problems considered in this chapter. Make a list of corresponding functions.
6. What does the complex potential give physically?
7. Write a short essay on the various assumptions made in fluid flow in this chapter.
8. Explain the use of conformal mapping in potential theory.
9. State the maximum modulus theorem and mean value theorems for harmonic functions.
10. State Poisson’s integral formula. Derive it from Cauchy’s formula.
11. Find the potential and the complex potential between the plates $y = x$ and $y = x + 10$ kept at 10 V and 110 V, respectively.
12. Find the potential and complex potential between the coaxial cylinders of axis 0 (hence the vertical axis in space) and radii $r_1 = 1	ext{ cm}, r_2 = 10	ext{ cm}$, kept at potential $U_1 = 200	ext{ V}$ and $U_2 = 2	ext{ kV}$, respectively.
13. Do the task in Prob. 12 if $U_1 = 220	ext{ V}$ and the outer cylinder is grounded, $U_2 = 0$.
14. If plates at $x_1 = 1$ and $x_2 = 10$ are kept at potentials $U_1 = 200	ext{ V}$ and $U_2 = 2	ext{ kV}$, respectively, is the potential at $x = 5$ larger or smaller than the potential at $r = 5$ in Prob. 12? No calculation. Give reason.
15. Make a list of important potential functions, with applications, from memory.
16. Find the equipotential lines of $F(z) = i 	ext{Ln } z$.
17. Find the potential in the first quadrant of the $xy$-plane if the $x$-axis has potential 2 kV and the $y$-axis is grounded.
18. Find the potential in the angular region between the plates $	ext{Arg } z = \pi/6$ kept at 800 V and $	ext{Arg } z = \pi/3$ kept at 600 V.
19. Find the temperature $T$ in the upper half-plane if, on the $x$-axis, $T = 30^\circ	ext{C}$ for $x > 1$, $T = -30^\circ	ext{C}$ for $x < -1$, and $T = 0^\circ	ext{C}$ for $-1 < x < 1$.
20. Interpret Prob. 18 as an electrostatic problem. What are the lines of electric force?
21. Find the streamlines and the velocity for the complex potential $F(z) = (1+i)z$. Describe the flow.
22. Describe the streamlines for $F(z) = -iz^2 + z$.
23. Show that the isotherms of $w = z^2$ are hyperbolas.
24. State the theorem on the behavior of harmonic functions under conformal mapping. Verify it for $\Phi^*(u, v) = e^u \sin v$ and $w = f(z) = z^2$.
25. Find $V$ in Prob. 22 and verify that it gives vectors tangent to the streamlines.

## SUMMARY OF CHAPTER 18 {#sec-ch18-summary}

Potential theory is the theory of solutions of Laplace’s equation

$$
abla^2 \Phi = rac{\partial^2 \Phi}{\partial x^2} + rac{\partial^2 \Phi}{\partial y^2} = 0$$ {#eq-sum-1}

Solutions whose second partial derivatives are continuous are called harmonic functions. Equation (1) is the most important PDE in physics, where it is of interest in two and three dimensions. It appears in electrostatics (Sec. 18.1), steady-state heat problems (Sec. 18.3), fluid flow (Sec. 18.4), gravity, etc. 

Whereas the three-dimensional case requires other methods (see Chap. 12), two-dimensional potential theory can be handled by complex analysis, since the real and imaginary parts of an analytic function are harmonic (Sec. 13.4). They remain harmonic under conformal mapping (Sec. 18.2), so that conformal mapping becomes a powerful tool in solving boundary value problems for (1), as is illustrated in this chapter.

With a real potential $\Phi$ in (1) we can associate a complex potential

$$F(z) = \Phi + i\Psi$$ {#eq-sum-2}

(Sec. 18.1). Then both families of curves $\Phi = 	ext{const}$ and $\Psi = 	ext{const}$ have a physical meaning. In electrostatics, they are equipotential lines and lines of electrical force (Sec. 18.1). In heat problems, they are isotherms (curves of constant temperature) and lines of heat flow (Sec. 18.3). In fluid flow, they are equipotential lines of the velocity potential and streamlines (Sec. 18.4).

For the disk, the solution of the Dirichlet problem is given by the Poisson formula (Sec. 18.5) or by a series that on the boundary circle becomes the Fourier series of the given boundary values (Sec. 18.5):

$$\Phi(r,	heta) = rac{1}{2\pi}\int_0^{2\pi}rac{(R^2-r^2)\,\Phi(R,lpha)}{R^2-2Rr\cos(	heta-lpha)+r^2}\,dlpha \quad (	ext{disk})$$

$$\Phi(x,y) = rac{y}{\pi}\int_{-\infty}^{\infty}rac{f(t)\,dt}{(x-t)^2+y^2} \quad (	ext{upper half-plane})$$

Harmonic functions, like analytic functions, have a number of general properties; particularly important are the mean value property and the maximum modulus property (Sec. 18.6), which implies the uniqueness of the solution of the Dirichlet problem (Theorem 5 in Sec. 18.6).
